console.log("AAAAAAHHH MOTHERLAND")

/**Implement a simple function named conway(cell, neighbors) that...
* Given the state of its neighbors
* Returns the state of cell after 1 tick
*/

function conway(cell, neighbors){ //true = alive, false = dead

  var neighbors =
  ["false", "false", "false",
   "false",          "false",
   "false", "false", "false"];


  if cell===true && neighbors === false;

  return "After 1 tick, the cell will die."
}

conway(true, false)
